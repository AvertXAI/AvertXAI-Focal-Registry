# STATUS-41.md

Product state, one line each. LIVE → PARKED → DEAD.
*(Supersedes STATUS-40 — delete it after upload. Rotated 2026-08-19: Scan Notes device-gated and green; 0.2.7 release in progress.)*
*(Focus 2026-08-19: **Scan Notes DEVICE-GATED and passing** — 415 of 415 stills, cold 6–8 s, steady fill, virtualized grid, background warm-up. **Next: release 0.2.7, then the Mission Control shell revamp.** Parked to 0.2.8: the manual rotate control, viewer focus-trap accessibility, the double-registered `diag:enabled` handler.)*
*(Focus 2026-08-18: **Scan Notes is BUILT and compiling green; the device gate is OWED** — nothing from 08-17/18 has rendered on Jason's screen. Sequence: device gate -> release 0.2.7 -> Mission Control shell revamp (recon written, not run) -> repackage. Five agents ran in parallel in this module; an adversarial review caught five defects pre-ship.)*
*(Focus 2026-08-17: **Scan Notes is the next Focal Registry build** — mockup v4 approved; read-only recon assigned to Claude Code desktop (`PROMPT-scan-notes-recon-08-17-2026.md`); build prompt after Jason rules the recon forks. Vault Tier-1 fixes remain queued per the 08-14 sequence — never run both on one working tree.)*
*(Focus 2026-08-14: **Focal Registry, sole focus** — 0.2.6 on the prerelease feed, main at `3e8a2d4`; Employees built through 3B.2; Complete Job + profit + invoices + purge fix shipped; Secured Vault standalone COMPLETE, mounting now. BuildersAudit beta tester waiting on same-app-or-separate. Lane: Claude Code desktop is THE build lane; Antigravity retired to surgical tasks.)*
*(Focus 2026-07-26: **Focal Registry is the sole active workstream.** 0.2.4 LIVE on the feed. MindMerge rename, Software Update window, and Migrate Phase 1 all shipped to `main`. runbooks.systems ON HOLD — everything consolidates onto Focal Registry.)*
*(Focus 2026-07-19: **Focal Registry is the active workstream** — Scan module SHIPPED end-to-end and proven on a 1.29M-file drive; Secure Note is next, mockup approved. runbooks.systems remains Active as the baseplate source. Prior focus notes retained below.)*
*(Focus 2026-07-04: **Runbooks is the active focus** — the foundation platform (all apps/docs/keystone consolidate here). Parallel build: **Claude-in-Antigravity → `D:\dev\AvertXAI-RunBooks\modules\`**, **Gemini → root**; merge when modules complete. GetScriptClips PAUSED mid-Phase-2.)*

## LIVE
- **GetScriptClips** — **PAUSED 2026-07-04** (was sole focus; resume anchor = Phase-2 Tracer-Bullet build). **Phase 1 architecture LOCKED 2026-07-01.** Android reel-to-text tool; V2 native Kotlin/Compose = THE app. On-device reel audio→text VERIFIED: capture via `AudioPlaybackCapture` + MediaProjection (default `ALLOW_CAPTURE_BY_ALL`; IG/TikTok/YT normal reel audio captures fine; DRM streams = per-content silence, caught via silent-buffer RMS + skip — NOT an app-wide block). **Audio-capture PROOF PASSED on-device 2026-07-01** (Run-1 RMS 4355 over 161,280 bytes on S26 — internal reel audio confirmed non-silent; the two silent runs = projection torn down mid-test / no media playing, not a capture defect). Runtime = whisper.cpp `tiny.en-q8_0`, NDK r26c, NEON (`-march=armv8.2-a+fp16`), CPU-only (no NNAPI/OpenCL). On-device primary + Hetzner WhisperLive WebSocket fallback on thermal throttle. Fixed 3–5s independent chunks + LocalAgreement-2 + VAD (NEVER a growing buffer). AccessibilityService DROPPED (Play landmine); MediaProjection FGS `mediaProjection` type + Android 14 per-session consent + terms gate (no Netflix/banking/DRM). Spec = `SPEC-GetScriptClips-Phase1-AudioPipeline.md`. **NEXT = Phase 2 Tracer-Bullet build** (capture service + C++ NDK/whisper.cpp bridge + chunking engine → transcribed string in logcat from a live reel). OCR R&D PRESERVED: capability SHIPS (conditional) — BIMODAL verdict — talking-head reels add <15% net-new over Whisper (Kinetic-Caption Trap), ad/product/POV-overlay/screen-record reels add large visual-only net-new; reconciliation (old B13 "subtract Whisper, keep visual") = later phase; SLM formatter + reject-don't-fix verifier = later phase, NOT decided. Engine spine + onboarding/UX phases device-verified DONE on S26 (per V2Port game-plan). Production Label Mode FROZEN at tag `labelmode-phase2.2a`; ocrtest harness at tag `ocrtest-public-mirror` = commit `b74162a` (the value `c71de0b9…` previously recorded as the SHA is an APK SHA-256, NOT a git commit — matching code commit = `a05a024`). Active audio work on branch `feature/audio-pipeline` (`7cf300f` parked audio-proof work, `5376063` sequential dev-only WAV naming); `main` clean at `b74162a`. Known deferred bug: crop insets read 0. Repo `D:\dev\AvertXAI-GetScriptClips`, pkg `com.avertxai.getscriptclips`; build+test = Android Studio/Gradle/ADB (NOT EAS). **NOTE 2026-07-15: an Apify-actor "buy the transcript, delete on-device ASR" pivot + a desktop fork are under EVALUATION (not locked) — resume direction may change; not written as a decision until Jason locks it.**

- **runbooks.systems** — **ON HOLD as of 2026-07-24** (was ACTIVE FOCUS 2026-07-04) (the foundation — everything consolidates here). Building **P1 Runbook-content module ("shredder")** — structural refactor DONE + green (`tsc`/`build` = 0, device-verified 2026-07-06): UI at `src/modules/runbook-shredder/`, main-process services at `electron/core/services/runbook-shredder/`, `/brain/`→`/services/` (DECISIONS-37), `--mc-` shell-token namespacing (RULES-27 CSS-token standard); NEXT = 3 UI bugs (sidebar-collapse hamburger, vertical divider, color-selection accuracy) in the module lane; cleanup pending (remove emptied root `modules/`, delete stray `src/globals.md`). Diet DONE (prod deps `{argon2, better-sqlite3-multiple-ciphers}`, installer 97.3MB, SQLCipher+argon2 proven in packaged binary). Standalone Electron ops platform (separate product, NOT a MissionControl module — decided 2026-06-28); encrypted vault + AI runbooks; web presence = runbooks.systems marketing URL only (Next.js). **Config-as-Data Phase 1 COMPLETE 2026-07-02** (Gemini/Fable-driven, reports 001–007): platform_registry gatekeeper, first-run wizard, dynamic module nav (MODULE_COMPONENTS slug→component registry), boot terminal, safe mode + persistent Retry, dev:clean. Repo `D:\dev\AvertXAI-RUNBOOKS` (this repo's Config-as-Data shell is the reusable "Mission Control shell"; Scout Viewer mounted here). **SHELL UPDATE 2026-07-15:** (1) sidebar now grouped into collapsible sections — **Applications** + **System** — via an additive `modules.nav_group` column, collapse state persisted in `app_settings` (key `nav_section_state`); COMMITTED `79da1f3` on branch `feature/crud-integrations`. (2) shell gained a **system tray** — window ✕ HIDES to tray (never quits), background stays alive, explicit Quit from the tray, single-instance lock restores the window; COMMITTED. (3) **Canon Distributor transfer IN PROGRESS** — mounted as the `canon-distributor` module under nav_group `System` (engine + 3-table schema `dist_source`/`dist_targets`/`dist_log` + 13 `dist:*` IPC + UI copied; NOT the old DB data; org-scoped via `org_id ?? modules.tenant_id`; Activity Log = internal icon-view; one sidebar entry) — currently in the **device-gate fix cycle** (light-theme token re-skin incomplete + source-set / watcher bugs), **NOT committed**. Branch `feature/crud-integrations` also still holds **uncommitted Scout Target CRUD**. **SHELL UPDATE 2026-07-17 (committed):** topbar single-row + breadcrumb-collapse-at-<900px COMMITTED `3fb5dd5`; Data Viewer render-side column reorder + compaction COMMITTED `170d8ee`; modal-dim COMMITTED `f70dd30`; **Phase-3 shell bundle COMMITTED `fb25dda`** — boot-frame terminal-dark (#0b0e16) all themes via boot-aware overlay funnel, boot-window lock + `setMinimumSize` re-assert on `boot:done` (Windows `setResizable` trap), `boot:done`/`boot:start` IPC on `window.runbooks`, first-run wizard body unified boot-dark, modal native-button inert. Shell floor `MIN_WIDTH=740` (JASON-APPROVED; docks at half-screen 960 with slack; do NOT restore 960; display profile 1920×1080 @ 100% scale, dpr 1). Resize-repaint edge gap = irreducible frameless-Windows artifact, ACCEPTED (all layers aligned byte-for-byte; `mica` rejected). Still UNCOMMITTED on `feature/crud-integrations` (separate gates): Canon Distributor Guardrails manifest (`ipc.ts` + `core/preload.ts` + `types.ts` + service/module + db additive cols), Scout Viewer guest-view parking + `tab-state.ts`, housekeeping (CANON rotations, docs, reports, `.claude`); branch never pushed, `origin/main` behind local. Dev/prod `userData` isolation still NOT implemented in this shell (root-lane task).

- **Focal Registry** — **ACTIVE, sole focus.** Photography archive tooling; alpha customer Paul Cruz. Repo `D:\dev\AvertXAI-Focal-Registry`, branch **`main`**, HEAD `3e8a2d4`, pushed. **0.2.6 LIVE on the prerelease feed** (`Focal-Registry-Setup-0.2.6.exe`). **Installed builds LAG main** — every device test runs `npm run dev` at current main or a fresh package, never the stale installed app. Builds remain UNSIGNED (Digital Signatures tab empty, confirmed 2026-07-26); `signtool.exe` lines in the build log are electron-builder's bundled tool with no certificate configured.
  - **Identity:** `productName` "Focal Registry" (was "AvertXAI Focal Registry"); `description` "Photography Archive Tools"; window title, tray tooltip (two lines), and `<title>` all match; `artifactName` `Focal-Registry-Setup-${version}` with the `release.mjs` pin moved in lockstep. `appId` and npm `name` untouched. `userData` now `%APPDATA%\Focal Registry`.
  - **Shell:** preload bridge renamed `window.runbooks` -> **`window.shell`** across all four consumers; IPC strings `boot:done`/`boot:start` unchanged; `window.api` untouched. `migrate.ts` and the legacy `OLD_SLUG = "runbooks"` migration DELETED after a disk check proved no legacy database existed. Repo-wide `runbook` grep in code = ZERO.
  - **MindMerge** (was Secure Note, was runbook-shredder) — slug `mindmerge`, own database `mindmerge_<org>.db`, IPC `mindmerge:*`, settings keys `mindmerge.*`, tables `mindmerge_notes`/`mindmerge_fts`, tokens `--mindmerge-*`. **Read-only ingest + search ONLY — there is NO editor and no authoring path.** The rebuild is APPROVED; **RULED 2026-08-14: the editor arrives by COPYING the Secured Vault's Secured Notes stack (Milkdown + Markdoc + autosave + import + search) into MindMerge after the vault mount** — queued directly behind the mount. The Repos online reader also lives here, never in the Vault.
  - **Software Update window** — SHIPPED in 0.2.4. Own `BrowserWindow` (560x540) with its own tiny `updateApi` preload (never receives `window.api`); frameless with themed `titleBarOverlay`; fixed non-scrolling summary box, scrolling details disclosure fetched MAIN-side via `net.fetch` (no CORS header needed on the feed), progress bar, external changelog link, Skip / Remind me later / Install. **Required mode** on a major bump: Install now / Quit only, non-closable, Quit sets `isQuitting` and `destroy()`s the window. **Unmaintained mode** at two-plus minors behind. Dev preview: `UPDATE_WINDOW_DEV=1|required|unmaintained npm run dev`.
  - **REVISIONS.md** — single source of release notes at repo root. `release.mjs` extracts the version's Summary (fails loudly on missing, placeholder, or over 400 characters), injects it into `prerelease.yml`'s `releaseNotes`, and uploads REVISIONS.md to the FEED ROOT. Step order is now `0/4 inject -> 1/4 payload -> 2/4 manifest -> 3/4 revisions -> 4/4 verify`.
  - **Migrate module** — Phase 1 SHIPPED to `main`, NOT user-facing until Phase 2. Slug `migrate`, nav position 3 (after Rename, before MindMerge). Tables `migrate_jobs`/`items`/`bundles`/`bundle_items` in the shared org DB, drive identity FK'd to `scan_drives`. Extension registry served as DATA, not hardcoded. Crash-safe walk pattern-copied from Scan with exact pre-count. Single-slot engine draining a job queue (tabs are renderer state). `DriveType` added for removable detection. Adobe-shipped files flagged and unticked. Bundle export to `<dest>\FocalRegistry\Bundles\<label>-MM-DD-YYYY\` with `manifest.json`, via `copyVerified` with hashing ON. Both preflight guards proven refusing: free-space, and destination-inside-source. **Phase 2 (install to Adobe/font folders) NOT started.**
  - **`copyVerified()`** — shared copy core at `electron/core/services/shared/copyVerified.ts`. `COPYFILE_EXCL`, source read-only, byte-count verify, optional SHA-256 DEFAULT OFF. Rename calls it with hashing off (proof harness re-run: FULL 17/17, CRASH-INSPECT 3/3, THE LAW held); Rename's REVERT path passes `verify:false` because it never size-verified and a user-edited copy must still revert. Migrate calls it with hashing ON.
  - **NOT STARTED:** MindMerge editor rebuild - Migrate Phase 2 - document/media viewer modals - delete and nuke - tips registry - folder-level extraction - custom search string - cancel-and-resume UI - device-identity capture - portable data relocation - Bugsink + Resend (need credentials) - folder watcher - the website (`AvertXAI-Focal-Registry-Website`, `focalregistry.com/releases`).
  - **KNOWN OPEN:** dev/prod `userData` isolation still NOT implemented — kill BOTH `electron.exe` AND `Focal Registry.exe` before any gate. `window.shell.version` is hardcoded `"0.1.0"` in preload, separate from package.json (no consumer displays it today). Video files never capture a camera (BL-23). `crash-test.ts` fixed counts predate media-only scanning (BL-26). `original_filename` is a current-stem fallback. `CANON/` reappears after deletion — the Distributor on another machine still targets it.

- **Focal Registry — RunBooks remnants: RESOLVED 2026-07-26.** All user-visible branding, the preload bridge, the org slug, the legacy migration, data-model names, CSS tokens, and provenance headers are gone. Repo-wide `runbook` grep in CODE returns ZERO. Remaining hits live only in canon, history documents (`GUT-LIST-focal-registry.md`), and past reports — deliberately untouched, because editing them would falsify a record.

- **AvertXAI-PhotoRename (legacy Python repo)** — `D:\dev\AvertXAI-PhotoRename`. Holds `frameshift_v16.py` (2,306 lines, active source), historical `frameshift_v*.py` + `cr2renamer_v5.py` (frozen, not git history), `brain/products/focal-registry-brand.md`, `CLAUDE.md`, `REVISIONS.md`, `BUILD.md` (stale pricing tiers), `requirements.txt` (`exifread==3.0.0`, `python-dotenv==1.0.1`), `icon.ico`. Baseline commit `a566991` ("Initial baseline: FRAMESHIFT v16 working state pre-Focal Registry rebrand", 17 files / 15,348 insertions); branches `main` + `focal-registry` both at `a566991`; **`origin/main` still at `49b7058` (empty initial commit) — local is 1 ahead, PUSH DEFERRED, never resolved.** Env: Python 3.14 PyManager Tcl/Tk path bug patched in two cooperating layers (script-level lines 14–26 + gitignored `venv/Lib/site-packages/sitecustomize.py`); venv rebuilt after an unrelocatable `A:\`→`D:\` move. **DISPOSITION UNDECIDED** — the Electron rebuild supersedes it, but the repo is the only home of the v16 logic and the brand file. Do not delete until the port is device-verified.

- **MissionControl / Jarvis** — **ARCHITECTURE LOCKED 2026-07-12, NO CODE YET.** The real product: Electron platform where an AI agent (Jarvis) helps users PLAN and BUILD software correctly; runbooks = the execution artifact. Stack locked (DECISIONS-41): Hermes Agent (MIT) runtime + echovault memory (Markdown+SQLite FTS5) + Cedar Security Guard + Structurizr/C4 contract + spec-kit plan gate + vault-rendered Wiki. 7 research phases complete (reports saved). Resume anchor = `HANDOFF-MissionControl-Jarvis-2026-07-12.md`. **NEXT = build the Obsidian working doc system** (Jason's tracker). Prior skeleton + generator (`npx avertxai-mission-control`) BUILT; Data Viewer module SHIPPED (`46f19e3`). Monorepo `D:\dev\AvertXAI-RunBooks.Systems\` (distinct from the runbooks.systems shell repo above).

## DECIDED — IN PROGRESS or PAUSED
- **Keystone Engine / Revenue Warden** (Auth · Entitlements · Billing · Subs) — **BUILT through P1, PAUSED.** P0 identity scaffold (Node/Fastify/TS-strict/Drizzle/Postgres, 26 tests incl. tenant isolation) + P1 profiles and entitlements MERGED (`d662dce`); Azure Key Vault ES256 signer merged (vault `avertxai-rw-signing`). Paused pending Jason: Q22 issuer strings, Q23–Q27, live-vault run, firewall stack (Cloudflare Tunnel + CrowdSec + Coraza, zero-cost). Lane: Claude Code web, own repo. (Old biometric CoreXAI/KEYSTONE renamed Legacy Keystone, parked.)
- **Secured Vault** — **STANDALONE BUILD COMPLETE 2026-08-14** (`modules/vault/` lane, Focal Registry repo, `feature/vault-build`, last source commit `79e0715` + polished uncommitted work; proofs green: engine 23 · notes 38 · redesign 20 · transfer 16 · codetheme 8). Surfaces: Passwords (versioned secrets, generator, health, access log) · Secured Notes (Milkdown + Markdoc, autosave, import, global search) · Infrastructure (DNS/servers/SSH keys/package ledger) · Repos (84-repo scan, README snapshots) · event log with request ids · vault-held settings · code-theme import. **MOUNT ASSIGNED 2026-08-14 to Claude Code desktop** — merge first, then `vault-broken-patch.md` fixes in order.
- **Scan Notes — RULED + mockup v4 approved 2026-08-17** (`MOCKUP-scan-notes-v4-08-17-2026.html`). Scan gains folder renaming (pending queue, auto-sync on connect), plaintext notes tables in the shared org database (Secured Notes stack copied, encryption removed), the Updated Notes feed tab, dual-tree markdown mirroring, and same-window media browse. **Recon assigned; build prompt after the recon forks are ruled.** The Scan header copy update rides the build.
- **Wiki** — DECIDED 2026-06-28, NOT STARTED. Standalone product (NOT a MissionControl module); knowledge/docs platform alongside Runbooks.

## PARKED
- **TimeTracker** — Electron 41 + React + TS desktop app. **REACTIVATING 2026-07-19: standalone revamp FIRST, dock into the Focal Registry shell as a module SECOND.** Concurrent-timer cap RULED 2026-07-26: **hard maximum 10 for users, uncapped for Jason.** Docking needs its own recon — separate repo. Jason wants design changes before it becomes a module; run and iterate it standalone until it hits the sweet spot, then dock. **Do NOT build a second time tracker** — the Focal Registry Project Management module consumes this engine and adds the project/proposal layer on top. Previously PARKED 2026-06-29. DUAL: standalone solo app AND a Client Matrix module. `4.7` multi-timer engine SHIPPED (schema v8, 151/151 smoke, HEAD `bf16915`, branch `feature/tracker-phase5` — 2 commits unpushed, NOT merged, .exe not repackaged). NEXT on resume = event-log gap (schema v9, ADD-only, Option A — log only, keep heartbeat); full crash-flag UI deferred to phase 9.1. Monorepo `D:\dev\AvertXAI-RunBooks.Systems\TimeTracker\`. Phase detail home = the 4.7.x handoff doc. **NOTE: `TimeTracker_v1/` is LIVE — tracking real hours; do not touch.** (2026-07-15: TimeTracker may also relocate into the runbooks/Mission-Control shell as a module — TBD, not decided.)
- **HappySmiles / DentalOS** — **PARKED 2026-06-29**. Standalone dental site (gethappysmiles.com); veneer barter (~$40K value); single-location; native CRM + AdminJS; build → pitch SA cosmetic/surgical + Reddit/Google ads.
- **Casa Sabor / Las Palapas** — **PARKED 2026-06-29** (demo stays deployed at demos.avertxai.com/casasabor). lacasasabor.com Phase 2 + Phase 2.5 deployed; Phase 3 pricing form + Resend + PostHog committed `aaab3d9` on phase3 branch, NOT pushed. Digital Ordering Fee = $1.50, broker model.
- **landing.avertxai.com** — **PARKED 2026-06-29** (form stays live). Developer-services lead capture; branch pushed; test email confirmed; 4 ad pixels (Meta/Google/Reddit/PostHog) + 800# swap still pending.
- **iLedger** — **PARKED 2026-06-29** (stays running on VPS). Windows registry install/uninstall tracker; iledger.avertxai.com.
- **Small-business RestaurantOS tier** — **PARKED 2026-06-29**. Pricing + onboarding survey + size-based billing schema (PostgreSQL); validation campaign incoming.
- **Jax's Collectibles** — **PARKED 2026-06-29** (client). Client site, ~40% functional/~88% visual; client unresponsive.
- ECUClone (unnamed) — ECU cloning / ROM map crowdsource platform; Electron + Python; alpha mechanic unresponsive; IDEAS-ECUCLONE.md; may build solo
- Dream — pain-point capture platform; name locked, no code; after Las Palapas
- PaperPluck — bank statements → CSV/IIF/Xero/JSON; validate via Reddit + Google Form first
- PASS Verified — course for SSI recipients; after Jason's own PESS/PASS approval
- Multi-tenant demo platform — serial-key vendor skins; post-pitch
- BuildersAudit — contractor auditing, Phase 3; validate via Reddit + Google Form first
- ThatsMe.Direct — NFC business cards; Paul Cruz admin
- PhantomProtocolPro — Chrome MV3 extension; SCUM game server vote automation
- Ctrl+X — Chrome MV3 tab session manager
- PhantomGate — internal lead-generation dashboard; 1,870+ records
- TokenSaver.app — AI model performance monitoring dashboard
- Legacy Keystone (was CoreXAI/KEYSTONE) — biometric face-to-hash, pre-patent; possibly dropped (several competitors now)
- **MindMerge.Network** — reactivated as Brand B: BYOK power tool (user's own API keys for AI summarization); separate from GetScriptClips carbon-neutral positioning; $29-49/mo or $99 one-time; domain owned ($14 sunk). Build after GetScriptClips ships.
- **GetScriptClips Chrome Extension** — companion web extension; 2 modes (Capture Region + Capture Tab) + sidebar library; local-first via chrome.storage/IndexedDB; backlogged as B-010. Build after Android ships.
- **Explosive Reps** — auto-logging workout app + Smart Lift Pin hardware sensor; domains explosivs.com + smartliftpin.com; hardware on hand (ESP32-S3, HX711, load cell); waiting on remaining parts before build.
- **Claude Usage Capture** — MissionControl personal widget; proven feasible (internal `usage` endpoint, cookie-auth, passive browser-extension capture → DB + averages); personal-use only; full spec `SPEC-claude-usage-capture-PARKED.md`; logged P-001 in `RESEARCH-AND-BACKLOG-CRM-1.md`. Build TBD.

## DEAD — do not propose
- MindMerge "personal AI OS" — DOA 2026-05-29 (domain reused for BYOK power tool above)
- CR2 Renamer Pro — name retired; now Focal Registry
- FRAMESHIFT — name retired; now Focal Registry. Current reference script = **`frameshift_v16.py`** (Tkinter, 2,306 lines) — canon was right, `v10` was an older upload. v16 adds a full MERGE ENGINE, recursive discovery, and `export_mode` that v10 lacks.
- Python/PySide6/Tkinter desktop UI for Focal Registry — retired 2026-07-18; Electron only
- Gitea / OneDev / any self-hosted git forge as an update host — rejected 2026-07-18; static file server only
- Gitea as canon archive — dropped; canon = project + local folder
- "AvertXAI CRM" as platform name — retired 2026-06-28; platform = MissionControl (AvertXAI = company)

## RunBooks shell — recon-verified reference values (2026-07-18)
- Geometry: `MIN_WIDTH=740 / MIN_HEIGHT=640` in `electron/core/windows.ts`, read by the constructor and re-asserted in `setBooting(false)`. **CONFIRMED CORRECT by Jason 2026-07-18** — the recon's "uncommitted, HEAD at 960" report is superseded; 740 is the value, do NOT restore 960. Rail expanded `--mc-flyout-width:300px` (drag-clamped ≤300), collapsed **58px**. Topbar height 58px is NOT a constant — three duplicated `calc(100vh - 58px)` literals across globals/shredder/scout stylesheets. Native strip 36px (`OVERLAY_HEIGHT`); topbar `padding-right:150px`. ONE container threshold `@container (min-width:900px)`. Single overlay writer verified (`applyOverlayNow`) — no defect.
- Shell theme tokens — hybrid / dark / light: `--mc-base` #0d1320/#1f1f1f/#FAFAFA · `--mc-panels` #121a2e/#2c2c2a/#FFFFFF · `--mc-nested` #182238/#2c2a26/#F5F5F5 · `--mc-border` #233149/#61615b/#E5E5E5 · `--mc-text` #e8edf7/#E5E5E5/#1A1A1A · `--mc-muted` #8b9bb4/#A0A0A0/#737373 · `--mc-accent-primary` #4f8df0/#d97757/#d97757 · `--mc-orange` #ff9100 (all) · `--mc-topbar` #0d1320/#262626/#FFFFFF · `--mc-sidebar` #121a2e/#262626/#FFFFFF · `--mc-field` #0d1320/#424240/#FFFFFF · `--mc-avatar` #4f8df0/#313131/#E6E6E6 · `--mc-dimmer` #5b6b88/#71716B/#A3A3A2 · `--mc-toggle-on` #4f8df0 (all) · `--mc-seg-on-background` #303539/#303539/#DCE8F5 · `--mc-seg-on-icon` #4773a6 (all) · `--mc-scrollbar-thumb` #2c3a58/#7c7b75/#B1B1B0. Single-value at `:root`: `--mc-flyout-width:300px` `--mc-boot-bg:#0b0e16` `--mc-underline:var(--mc-accent-primary)` `--mc-green:#16a34a` `--mc-radius:6px` `--mc-font` `--mc-mono`. Module-scoped: `--scout-danger #e0565c` `--scout-bookmark-saved #d4a843` `--canon-distributor-{error #e0565c, favorite #d4a843, pending #d4a843, replace #4773a6}` `--shredder-highlighter-{background #ffe14d, foreground #1a1500}` (on `.rbs-shell`, not `:root`).
- Config-as-Data: `modules` table = std id/uuid/created_at/updated_at + `tenant_id` TEXT NOT NULL, `name`, `slug` UNIQUE, `type`, `display_order` INT default 0, `is_locked` INT default 0, `is_enabled` INT default 1, `nav_group` TEXT (additive guarded ALTER, NULL backfilled to 'Applications'). `RENDERER_KEYS` (12): skip_fast_boot, rail_collapsed, flyout_width, last_active_module, nav_section_state, watcher_enabled, theme_mode, org_name, runbook-shredder.{watch_path, watch_enabled, rail_collapsed, font_size}. `localStorage` references = ZERO (comments only).
- Known shell defects (unfixed in RunBooks; a new repo should fix during its gut): 30+ hardcoded hex/rgba literals in globals.css outside token declarations; boot dark #0b0e16 duplicated in 4 places synced by comment only; 58px header in 3 stylesheets with no shared token; `diag:enabled` throws a benign console error on every normal run; TWO coexisting push-subscription mechanisms (dist on/off+whitelist vs scout subscribe()); `dataviewer_dev_mode` bypasses RENDERER_KEYS by design; `window.runbooks.version` hardcoded separately from package.json; dev/smoke files in the production tree (markdown.smoke.tsx, smoke.ts, diag-read.mjs); uncommitted MC_GPU probe in main.ts.
- `electron-updater` is NOT PRESENT in the RunBooks tree — zero code, zero deps (prose mention only in KEYSTONE-REQUIREMENTS.md). Any implementation is greenfield.
- UNTRACED, do not assume dead: `electron/core/services/runbooks/` (`runbooks:list`/`create`, `api.runbooks`) had no renderer consumer in a grep pass; `src/keystone/` is docs-only. Both flagged UNCERTAIN by the recon, not proven.
- **Recon reliability:** the 2026-07-18 reconnaissance was VERIFIED WRONG on Settings page structure. Its file-existence and grep claims are sound; its RENDERED-STRUCTURE claims require on-device confirmation before driving an edit.

## Focal Registry — v16 source findings (2026-07-18)
- `frameshift_v16.py` is a FOUR-layer single file: rename engine · **merge engine** · EXIF · Tkinter GUI. v10 had only rename + EXIF + GUI.
- **Merge engine (NOT in v10, NOT yet scoped as an Electron module):** `gather_all_files` (recursive) · `gather_all_files_excluding` · `group_files_for_merge` · `build_merge_preview` · `execute_merge` · `verify_and_delete_sources` · `load_latest_log`. It CONSOLIDATES a scattered folder tree into one destination folder, renaming on the way.
- **Merge safety invariants that MUST survive any port:** (1) verify after every move — destination exists AND size matches source, else raise; (2) cross-drive moves fall back to `shutil.copy2` → verify size → `unlink` source, NEVER a blind move; (3) the transfer-log JSON is rewritten after EVERY file so a crash mid-transfer keeps the record (`transfer_<ts>.json`, per-entry original_path / original_name / new_name / destination / size_bytes / verified / error / timestamp); (4) `verify_and_delete_sources` deletes a source folder ONLY when it contains zero files of any type, walks deepest-first, and never touches the destination or any ancestor of it.
- v16 rename layer also gained `discover_files(recursive=)` and `execute_renames(export_mode='inplace'|...)` — the recursion and copy-vs-move questions were already answered in the Python source.
- Known v16 defects to fix in the port (carried from v10): `{date}` and `{shotdate}` both resolve to the same `date_str` slot; FRAMESHIFT's own colour dict (`#070d1a` / `#0d1422` / `#1a2d45` / `#3b7fff` / `#35d499`) is a THIRD palette — discarded, Mission Control tokens win.

## Dev environment gotchas (2026-07-18)
- **`ELECTRON_RUN_AS_NODE=1` is inherited from a VS Code process tree** — launching Electron from a VS Code-spawned terminal fails with `TypeError … createFromDataURL of undefined` plus a Node.js version footer. Clear the variable, or launch from a normal terminal. Not a code defect; costs a debug cycle every time it is met.
- `%APPDATA%` = `AppData\Roaming`, NOT `AppData\Local` — Electron `userData` lands in Roaming (CONFIRMED 2026-07-18). Checking Local and finding nothing is not evidence of absence.

## Infrastructure (running)
- Hetzner VPS + Coolify v4.0.0 (core.avertxai.com); MetaMCP, N8N, PostgreSQL, Redis, SeaweedFS, Immich, iLedger, Ntfy; Traefik proxy/SSL; Cloudflare DNS only. (Gitea REMOVED from VPS 2026-06-27 — source control = GitHub only, all repos private.)
- Monorepo home: D:\dev\AvertXAI-RunBooks.Systems\ (MissionControl shell + TimeTracker + modules, one git repo)
- Remote: Chrome Remote Desktop (phone ↔ dev PC "AvertXAI-WebDev") active for off-desk supervision

## Domains
- lacasasabor.com — public Casa Sabor / Las Palapas marketing site
- demos.avertxai.com — Casa Sabor demo at /casasabor; remains for other demo work

---

## Focal Registry — CURRENT STATE 2026-08-14

**LIVE:** 0.2.6 on the prerelease feed; `origin/main` = `3e8a2d4`. Commit chain this cycle: `ce51efc` (Employees 3B) → `c952c86` (3B.2 wizard + employee timer) → `62206ec`/`f57edaf` (project-modal rework) → `b451944` (seed + schema-at-birth) → `4465c65` (13-item fix pass) → `e403b76` (Complete Job + profiles + invoice numbering + dev leaf) → `1a87d67` (invoice skill compliance, Inter embedded) → `d078aae` (profit build) → `3e8a2d4` (purge fix). Working-tree route: commit on `feature/vault-build`, cherry-pick to `main` in a temp worktree, push.

**Employees:** BUILT through 3B.2 (people CRUD, Add Time, employee timer, both TimeTracker placements). 3C (Tasks · Adjustments · Details) and Payroll OPEN; Payroll parked until Jason sits with it. Two small fix rulings queued: four address columns on `timetracker_clients`; `phone_ext` moves beside the phone it extends.

**Secured Vault:** standalone COMPLETE (see the entry above). Mount assigned; wizard build (Personal/Business fork, profile fields, master-password one-time change, demo seed + modal) rides in the same assignment; `vault-broken-patch.md` is the post-merge fix map.

**Device gate:** FIVE checklists stacked and never run — nothing from 3A through the purge fix has fully rendered on Jason's screen beyond leaf/seed/purge spot-checks. Invoice PDF, three-theme passes, analytics visuals, completion-rail behaviour: Unknown until gated on `npm run dev` at `main ≥ 3e8a2d4`.

**BuildersAudit:** beta tester found and excited, wants fake live data. OPEN: same app reskinned for contractors, or separate product. Demo lead: complete-job → did-you-get-paid → profit.

**Known open:** dev/prod `userData` isolation not implemented · `focalregistry.com/releases` does not exist while the app links to it · FR scoped canon STALE (`FR-CANON-5` on disk vs `FR-CANON-1` named in CLAUDE.md §0.1 — regenerate to FR-CANON-6 and reword §0.1 to "highest-numbered") · root `CANON-UPDATES.md` prune (27 → 12) was the 08-10 session's deliberate applied-entry prune, commits with this rotation · vault-lane ledger (17 entries) merges into the root ledger at mount.

**Dead — do not propose:** the Scan god-mode surface (wizard, search, extraction, viewers, delete, nuke) · anything named `runbooks` · org-database encryption · a $99 one-time price for Focal Registry · automating a bank payment send.

## Focal Registry — Scan Notes (BUILT 2026-08-18, device gate OWED)
- **Shipped this cycle:** per-folder notes and reports · folder rename with full history and reconnect queue · dual-tree markdown mirroring · Updated Notes feed · Recent Work · empty-folder hiding with toggle · three-state empty messages · search across old and new folder names, notes and reports, grouped by kind · media wall with disk-cached thumbnails · media viewer (still/video/audio, next-previous, filmstrip, expand, Show in Explorer, Copy path) · `frmedia` streaming with no size cap · RAW previews for CR2 and CR3 · classified failure log with retry · developer-tools shortcut owned.
- **Measured on device:** 85 of 85 video thumbnails paint, `failed 0`; CR3 262 of 262 previews; CR2 415 files, no regression.
- **OWED — the whole surface is unrendered.** Device gate at 740 and 1440, all three themes, rail expanded and collapsed. Nothing here has been seen on screen.
- **NOT built, deliberately:** RAW-plus-jpeg grouping · embedded cover art for audio tiles · shot date and camera in the viewer footer (the media record does not carry them) · forward navigation (the shell owns it).

## Mission Control shell revamp — RULED, recon written, NOT run
- Reference approved (`BuildersAudit_Shell_Reference_v1_dc.html`), rulings in DECISIONS-54, read-only recon prompt written. **Runs after 0.2.7 ships.** The frameless-window fork is open and Jason's to rule from the recon's cost report.

## Release
- **0.2.7 PREPARED, NOT SHIPPED** — release prompt written; it stops before the single publish command per canon. 0.2.6 remains the live prerelease build; Paul auto-updates from it.

## Focal Registry — Scan Notes, device-gated 2026-08-19
- **PASSED on device.** `Day 1 - Jason` shows all 415 stills (was 250 — the clamp hid 165), cold in 6–8 seconds, filling at a steady rhythm rather than stalled clumps. RAW instant from cache.
- **Shipped since STATUS-40:** worker-window thumbnails (`b77e0ac`, 2.15× landscape / 1.69× portrait, 8/8 orientation fixture) · job-token cancellation (`11e0164`) · the 500-row clamp and its four hidden caps (`0bdef1e`, `getMany` 926 ms → 154 ms, payload 27.2 MB → 7.2 MB) · sibling reuse with the capture-time guard (`9ae7a29`) · virtualized grid + background warm-up (`b4d2d9b`, ~415 elements → ~40–60).
- **0.2.7 release IN PROGRESS** — gate, commit, version bump, `REVISIONS.md` for Jason's approval, package; publish is his single command. 0.2.6 is what Paul currently runs.
- **Parked to 0.2.8:** manual rotate control (automatic orientation makes it a backstop, not a fix) · viewer declares `aria-modal` with no focus trap or restore · `diag:enabled` registered twice and throwing at boot under `DIAG=1` (pre-existing).
- **Unproven, watch at the next gate:** whether Chromium decodes a `<video>` parked on the off-screen bench. If video tiles glyph where they used to paint, that bench is the cause — one CSS rule.
